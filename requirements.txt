# Absolute Blockchain Node — зависимости
# Ядро работает на чистой стандартной библиотеке Python 3.10+
# Все обязательные зависимости ниже

# ── Обязательные ─────────────────────────────────────────────────────────────

# Криптография (ECDSA подписи — используется в crypto/)
ecdsa>=0.19.0

# ── Опциональные (WebSocket + тесты) ─────────────────────────────────────────

# WebSocket поддержка (для будущего ws_server)
websockets>=12.0

# HTTP-клиент (для оракулов, бридж-запросов)
requests>=2.31.0

# JWT (промышленный профиль API)
PyJWT>=2.8.0

# Тестирование
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ── Для разработки (необязательно) ───────────────────────────────────────────

# Форматирование кода
# black>=24.0.0
# flake8>=7.0.0

# Дополнительная крипто (если понадобится AES/SHA3/RIPEMD)
# pycryptodome>=3.20.0

# FFI для Rust-моста (когда будет компилироваться)
# cffi>=1.16.0
